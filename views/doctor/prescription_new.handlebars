<main>
{{!-- <br/>  --}}

  <div class="w3-container medi-yellow">
    <h2>{{title}}</h2>
    {{!-- <p class="extra"><a href="/signup">(Don't have an account? Sign Up</a>)</p> --}}
  </div>

          <br>

          <div class="w3-container">
            <table id="booking_info_table" class="info_table">
              {{!-- <tr>
                <th>Patient</th>
                <th>Info</th>
                
              </tr> --}}
              <tr>
                <td>Booking ID</td>
                <td>#{{reservation._id}}</td>
              </tr>
              <tr>
                <td>Date</td>
                <td>{{reservation.date_formatted}}</td>
              </tr>
              <tr>
                <td>Patient Frist Name</td>
                <td>{{reservation.patient.fname}}</td>
                
              </tr>
              <tr>
                <td>Patient Last Name</td>
                <td>{{reservation.patient.lname}}</td>
              </tr>
              <tr>
                <td>Patient Email</td>
                <td>{{reservation.patient.email}}</td>
              </tr>
              <tr>
                <td>Doctor Name</td>
                <td>Dr. {{reservation.doctor.name}}</td>
              </tr>
              <tr>
                <td>Diagnosis</td>
                <td>
                  <textarea class="w3-input" name="diagnosis" style="resize: none;"></textarea>
                </td>
              </tr>
              <tr>
                <td>Medicines</td>
                <td>
                  <select multiple class="w3-input select-50" name="medicines" id="medicines" price="0">
                    {{!-- <option></option> --}}
                    {{#each medicineList}}
                    <option value="{{this._id}}" price="{{this.price}}" {{#if this.selected}} selected {{/if}}>{{this.name}}   <span class="price-span">(${{this.price}})</span></option>
                    {{/each}}
                  </select>
                </td>
              </tr>
              <tr>
                <td>Room</td>
                <td>
                  <select class="w3-input select-50" name="room" id="room" price="0">
                    <option value="" price="0"></option>
                    {{#each roomList}}
                    <option value="{{this._id}}" price="{{this.price}}" {{#if this.selected}} selected {{/if}}>{{this.name}} (${{this.price}})</option>
                    {{/each}}
                  </select>
                </td>
              </tr>
              
              <tr>
                <td>Cost</td>
                <td id="total_cost" price="0"></td>
              </tr>
              
          </table>

            <br>
            <button class="button1" type="button" onclick="alert('this will save the prescription')">Save</button>
          </div>


          

</main>